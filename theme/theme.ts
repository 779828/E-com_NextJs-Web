import { createTheme } from "@mui/material/styles";

export const lightTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#1e293b",
    },
    background: {
      default: "#f9fafb",
    },
  },
});
